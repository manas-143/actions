name: Python Hello World

on:
  push:
    branches:
      - main
jobs:
    runs-on: ubuntu-latest

    steps:
     - name: Checkout code
       uses: actions/checkout@v3

     - name: Run Python script
       run: python p1.py
